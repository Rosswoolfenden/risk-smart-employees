import React, { useState } from "react";
import { HomeIcon, Bars3Icon, XMarkIcon } from "@heroicons/react/20/solid";

const Navbar = () => { 
    const menu = [
        {
            name: 'Home',
            url: '/'
            
        }
    ]
};